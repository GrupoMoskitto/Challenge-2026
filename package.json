{
  "name": "crmed",
  "version": "1.0.0",
  "description": "Sistema Inteligente de Relacionamento e Performance Clínica - Hospital São Rafael",
  "private": true,
  "scripts": {
    "infra:up": "cd infra/docker && docker-compose up -d postgres redis localstack",
    "infra:down": "cd infra/docker && docker-compose down",
    "infra:postgres": "docker run -d --network host -e POSTGRES_USER=crmed -e POSTGRES_PASSWORD=crmed123 -e POSTGRES_DB=crmed postgres:16-alpine",
    "infra:db:setup": "pnpm infra:postgres && sleep 5 && pnpm --filter @crmed/database db:generate && pnpm --filter @crmed/database db:migrate",
    "infra:whatsapp": "cd infra/evolution-api-local && npm run db:deploy && npm run db:generate && npm run start:prod",
    "infra:dev": "pnpm infra:up && pnpm --filter @crmed/database db:push && pnpm --filter @crmed/database db:seed && concurrently \"pnpm infra:whatsapp\" \"pnpm dev\"",
    "dev": "kill-port 8080 3001 3000 || true && turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "vitest run"
  },
  "keywords": [],
  "author": "Grupo Moskitto",
  "license": "ISC",
  "packageManager": "pnpm@10.30.1",
  "devDependencies": {
    "@types/node": "^25.3.5",
    "@vitest/coverage-v8": "^4.0.18",
    "concurrently": "^9.2.1",
    "kill-port": "^2.0.1",
    "turbo": "^2.8.10",
    "vite-tsconfig-paths": "^6.1.1",
    "vitest": "^3.2.4"
  }
}
